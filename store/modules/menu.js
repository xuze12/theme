import Menu from '../../data/menu'

// import Menu from '../../data/xz_menu'

const state = {
  data: Menu.data
}

// getters
const getters = {
}

// mutations
const mutations = {
}

// actions
const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
